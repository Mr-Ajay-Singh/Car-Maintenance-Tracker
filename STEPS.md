1. Add the given features in the PRODUCT_REQUIREMENTS.md file currently there is plan of different app in it make it this plan
2. Make the folder for each feature planned (PRODUCT_REQUIREMENTS.md) inside lib/features folder and add all the folders required inside it following the CLAUDE.md file rules
3. Copy UI.md file in each of the features folder lib/features/featureName there only
4. Copy PLAN.md file in each of the features folder lib/features/featureName there only
5. Edit the PLAN.md file and do it according to our plan for that feature only and  take the plan from PRODUCT_REQUIREMENTS.md and from the folderName get the exact and write the complete plan with code following CLAUDE.md it should contains only the logic part not UI like service class and data class.
6. Start implementation of the first PLAN.md file of the features folder and then ask to continue and move to next and so on.
7. Edit the UI.md file of each features lib/features/featureName based on the PRODUCT_REQUIREMENTS.md , CLAUDE.md and PLAN.md and only change the logic part don’t change the apptheme thing and some fixed thing that is not part of the logic like each of the features (lib/features/featureName) should contain that start from one and once completed once ask for next and go to next.
8. Start Implementing the UI.md file and keep the UI logic separate for each of the UI.md planned thing like widgets should be in different file not same file. And start from the first and once that is completed ask for the next then i will tell then go to next UI.md of the features folder.
9. Now create the full route fixes and routing for each features should be there and inside app there should be bottom navigation bar and side navigation bar that will be used for the navigation of the feature.
10. Now fix all the linter errors which are there in the app and if anything mission or any other thing just add those things in the MISSING_ITEM.md file
11. Create an APP_STORE_LISTING.md file where create the app name under 30 character, short description of under 80 characters and full description under 4000 character and each of them should be ASO friendly.
